import { ImageDataPipe } from './image-data.pipe';

describe('ImageDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageDataPipe();
    expect(pipe).toBeTruthy();
  });
});
